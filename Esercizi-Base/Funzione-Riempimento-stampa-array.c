#include <stdio.h>
#define N_MAX 100
void readArray(int dim,int v[]);
int main(){
	int dim;
	int v[N_MAX];
	printf("Inserisci la dimensione desiderata per l'array:");
	scanf("%d",&dim);
	readArray(dim,v);
}

void readArray(int dim,int v[]){
	printf("Inserisci ")
}
